# Файлы, которые PhotoArchive.exe создаёт рядом с собой при запуске -- не часть репозитория.
photoarchive_config.yaml
work.db
work.db-journal
crash.log
crash.log.*
analyze_report.csv
__служебные_файлы/
PhotoArchive.exe
__pycache__/

# Bundled external binaries: intentionally NOT committed, see bin/README-BIN.md
bin/*
!bin/README-BIN.md

# PyInstaller build output (build.bat runs from build/, producing build/build/ + build/dist/)
build/build/
build/dist/
dist/
*.spec
